import type { Href } from 'expo-router';

/** Typed-route safe paths until `expo start` refreshes `.expo/types`. */
export const hrefAuthLogin = '/(auth)/login' as Href;
export const hrefAuthRegister = '/(auth)/register' as Href;
